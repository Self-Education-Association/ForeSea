@{
    ViewBag.Title = "Home Page";
    TempData["CloseTab"] = true;
}

@using (Html.BeginForm("Index", "Home", FormMethod.Post))
{
    <div class="form-horizontal">
        <h4>签到</h4>
        <a class="text-danger">dog pear country alive die by</a>
        <br />
        <a class="text-danger">_(:з」∠)_召唤龙泉寺大神orz</a>
        <hr />
        <br />

        <div class="form-group">
            @Html.Label("这里输你的st账号，st账号知道吧。", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.Editor("id", new { htmlAttributes = new { @class = "form-control" } })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="签到" class="btn btn-default" />
            </div>
        </div>
    </div>
}
